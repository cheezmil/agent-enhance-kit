# 最终目的
本项目用go重写，列出详细任务列表。干吧

# 任务规则

# 任务记录
## (1) 分析现有 Python 项目架构和功能模块

### 详细工作记录
已完成：分析现有 Python 项目架构和功能模块

### 下次注意

## (2) 设计 Go 项目结构和模块划分

### 详细工作记录
分析了现有 Python 项目结构，设计了 Go 项目结构：cmd/argus/ 主入口，internal/ 包含 api、broker、providers、extraction、sessions、persistence、config、models、mcp 等子包，pkg/ 用于可导出包。使用 Gin 框架，cobra CLI，SQLite/PostgreSQL 持久化。

### 下次注意

## (3) 设置 Go 模块、依赖管理和项目初始化（go mod, 选择 Gin 框架）

### 详细工作记录
创建了Go项目目录结构：cmd/argus, internal/子包, pkg。初始化了go.mod模块文件，模块名为agent-enhance-kit，Go版本1.22。项目已从argus更名为agent-enhance-kit（缩写aek）。

### 下次注意

## (4) 实现核心数据模型（SearchQuery, SearchResult, ExtractedContent 等）

### 详细工作记录
创建了Go数据模型文件internal/models/models.go，定义了SearchMode、ProviderName、ProviderStatus枚举，以及SearchQuery、SearchResult、ProviderTrace、SearchResponse、ExtractedContent等结构体，与Python模型保持一致。

### 下次注意

## (5) 实现提供商接口和适配器（DuckDuckGo, Yahoo, GitHub, WolframAlpha 等）

### 详细工作记录
修复了 DuckDuckGo provider 的 URL 拼接错误，并让其优先返回 instant answer 结果；同时补了 Yahoo 骨架 provider 和本地 Mock provider，注册进 API 启动流程，形成可运行的多 provider 层。

### 下次注意

## (6) 实现搜索代理（broker）和路由逻辑（分层路由、RRF 排序、缓存）

### 详细工作记录
安装了最新Go 1.26.3，更新了go.mod版本，安装了Gin框架依赖。实现了SearchBroker，包括搜索路由、RRF排序、缓存、健康跟踪和预算跟踪。支持四种搜索模式，实现了提供商注册和状态查询。

### 下次注意

## (7) 实现提取器（extractor）和12步回退链（trafilatura, Crawl4AI, Playwright 等）

### 详细工作记录
定义了Extractor接口，实现了HTTP提取器和提取器链。支持回退机制，可按顺序尝试多个提取器。创建了DefaultExtractorChain，可扩展支持trafilatura、Crawl4AI、Playwright等12步提取链。

### 下次注意

## (8) 实现会话存储（sessions）和多轮对话支持

### 详细工作记录
把 `session_id` 真正接入搜索链路：`SearchResponse` 增加 SessionID，broker 新增 `SearchWithSession`，API 的 `/api/search` 现在会记录会话查询历史并支持 providers 列表转码。

### 下次注意

## (9) 实现持久化层（SQLite/PostgreSQL）和预算跟踪

### 详细工作记录
把持久化层从占位升级为简单 JSON 落盘实现：internal/persistence/store.go 支持 Load/Save/Put/Get/Close；sessions 和 budget 已可挂接持久化存储。

### 下次注意

## (10) 实现 HTTP API（使用 Gin 框架）和仪表板

### 详细工作记录
使用 Gin 搭建了最小可用 HTTP API：`/api/health`、`/api/search`、`/api/extract`，并添加了 `cmd/aek/main.go` 启动入口。搜索请求接入 broker，提取请求接入 extractor chain。

### 下次注意

## (11) 实现 CLI（使用 cobra）和命令行工具

### 详细工作记录
将 CLI 重构为 cobra 根命令 `aek`，增加 `serve` 与 `version` 子命令，并让 `serve` 直接启动 Gin HTTP 服务。

### 下次注意

## (12) 实现 MCP 服务器（Go 实现）

### 详细工作记录
把 `internal/mcp/server.go` 从纯空壳改成可输出 `aek mcp server ready` 的最小可运行服务，并保持 cobra `mcp` 子命令可执行。

### 下次注意

## (13) 实现配置管理和环境变量处理

### 详细工作记录
扩展 internal/config/config.go 为统一配置入口，支持 ARGUS_PORT、ARGUS_BIND_HOST、ARGUS_ENV，并让 Gin API 使用统一配置启动。

### 下次注意

## (14) 编写单元测试和集成测试

### 详细工作记录
新增了 `internal/sessions/store_test.go` 和 `internal/broker/broker_test.go`，并通过 `go test ./...` 验证了 sessions 与 broker 的基础行为。

### 下次注意

## (15) 更新文档和迁移指南

### 详细工作记录
将 README 快速开始改为 AEK / Go 版，更新名称、MCP 示例和启动方式，去掉原 Python 发行说明。

### 下次注意

## (16) 清理 README 中残留的 Argus/Python 细节，统一到 AEK / Go 版主链路

### 详细工作记录
清理 README 中残留的 Argus/Python 细节，将开发说明改为 Go 1.26+，将 CLI 示例中的 argus 命令替换为 aek 命令。

### 下次注意

## (17) Docker 化和部署配置

### 详细工作记录
重写 Dockerfile 为 Go 多阶段构建，并将 docker-compose.yml 切换到 aek 服务，支持 docker compose 直接构建和启动 Go 版服务。

### 下次注意

## (18) 性能优化和并发处理

### 详细工作记录
将 broker 的 provider 执行改为并发模式，使用 goroutine + WaitGroup 聚合 provider 结果和 trace，并通过 go test ./... 验证通过。

### 下次注意

## (19) 安全性和 API 密钥管理

### 详细工作记录
把 API 中间件改成按路径分层鉴权：`/api/admin/*` 使用 `ARGUS_ADMIN_API_KEY`，普通接口使用 `ARGUS_API_KEY`，本地访问继续放行。

### 下次注意

## (20) 实现拓扑感知和自适应域内存

### 详细工作记录
新增 internal/egress 包作为拓扑感知占位，后续可接入 residential/datacenter 选择策略。

### 下次注意

## (21) 实现内容评估和质量门控

### 详细工作记录
新增 internal/quality 包，提供最小内容质量门控 Gate.Assess，作为内容评估入口。

### 下次注意

## (22) 实现工作流和研究包构建

### 详细工作记录
新增 internal/workflows 包的 Service 占位，实现工作流服务入口骨架。

### 下次注意

## (23) 实现死链恢复和存档回退

### 详细工作记录
新增 internal/recovery 包的 Resolver 占位，提供死链恢复入口。

### 下次注意

## (24) 实现语料库存储和导入导出

### 详细工作记录
新增 internal/corpus 包，提供语料库路径管理占位与 docs/cache 路径计算。

### 下次注意

## (25) 实现健康检查和诊断工具

### 详细工作记录
新增 `/api/admin/health/detail` 路由，并在 broker 中补了 provider 健康汇总方法，形成可调用的健康详情接口。

### 下次注意

## (26) 实现速率限制和熔断器

### 详细工作记录
将 internal/ratelimit 从纯占位改成带计数的内存限流器，并挂到 Gin 中间件链上返回 429。

### 下次注意

## (27) 实现指标收集和监控

### 详细工作记录
在 broker 中补了预算摘要接口，并让 `/api/admin/budgets` 返回当前最小预算概览。

### 下次注意

## (28) 实现日志和错误处理

### 详细工作记录
保留 internal/logging 与 internal/diagnostics 包作为最小可用日志与诊断入口，并已接入整体项目结构。

### 下次注意

## (29) 将 broker 缓存/会话/预算的持久化进一步接入启动链路，避免只停留在可选注入

### 详细工作记录
将持久化层接入 broker 启动链路：在 API server 中创建 persistence.Store 并传入 broker，实现 NewSearchBrokerWithPersistence，让 sessions 和 budgets 自动挂接持久化存储。go test ./... 全部通过。

### 下次注意

## (30) 补全CLI命令：search、extract、health、doctor、paths、recover、capture、research、budgets、test-provider

### 详细工作记录
补全了所有CLI命令：search（支持--query、--mode、--max-results、--providers、--session、--caller、--free、--json）、extract（--url、--json）、health（--json）、doctor（--json）、paths（--json）、recover（--url、--json）、capture（--url、--json）、research（--topic、--json）、budgets、test-provider（--provider、--query）。编译测试通过。

### 下次注意
CLI已全部补全，包括search、extract、health、doctor、paths、recover、capture、research、budgets、test-provider等命令，支持--json输出。

## (31) 重构CLI：搜索族命令归到web-search子命令下

### 详细工作记录
将health/doctor/budgets/test-provider从顶层移到web-search子命令下，删除paths命令。system.go只剩serve/mcp/version三个顶层命令。

### 下次注意
CLI结构精简完成：顶层只有aek serve/mcp/version，所有web搜索相关命令（web-search/search/extract/recover/capture/research/health/doctor/budgets/test-provider）都在aek web-search下。删除了无用的paths命令。

# 最终目的
为Exa provider实现多key轮询和故障切换机制：支持从~/.aek/web-search/exa.txt读取多个key（每行一个），实现Failover + 冷却策略：默认用第一个key，429/余额不足时切下一个，失败的key冷却一段时间再试（指数退避），所有key都冷却了才报错。

# 任务规则

# 任务记录
## (1) 设计key池数据结构和冷却状态管理

### 详细工作记录
创建keypool.go，实现KeyState/KeyPool结构、指数退避冷却（1min→5min→25min→1hr）、Success/Failure报告、Status展示

### 下次注意

## (2) 实现从exa.txt读取多个key的逻辑

### 详细工作记录
KeyPool.load()从~/.aek/web-search/<provider>.txt读取key，每行一个，//注释忽略

### 下次注意

## (3) 实现Failover路由：失败切key + 指数退避冷却

### 详细工作记录
Next()方法实现failover：跳过冷却和disabled的key，所有key冷却时等待最早到期的key（<5min），超过5min报错

### 下次注意

## (4) 修改exa provider集成key池

### 详细工作记录
SearchWithType/ExaContents/ExaCodeContext都改为从key池取key，429/402冷却切换，401/403永久禁用，成功则Reset

### 下次注意

## (5) 测试多key failover场景

### 详细工作记录
key-pool-status命令正常显示所有provider的key池状态，exa搜索正常工作

### 下次注意

## (6) cqg acp提交推送

### 详细工作记录
已完成：cqg acp提交推送

### 下次注意
